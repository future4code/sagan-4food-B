export const setDialog = (option) => ({
    type: "SET_DIALOG",
    payload: {
        option
    }
})

export const setItemId = (itemId) => ({
    type: "SET_ITEM_ID",
    payload: {
        itemId
    }
})